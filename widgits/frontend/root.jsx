import Clock from './clock';
import React from 'react';
import Tabs from './tabs';

const Root = function () {
    return ( 
      <div>
      < Clock />
    < Tabs /> 
    </div>);
};

export default Root;